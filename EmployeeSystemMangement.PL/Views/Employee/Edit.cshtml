@model EmployeeViewModel
@{
    ViewData["Title"] = "Edit";
}

<h1>Edit Employee</h1>
<form asp-action="Edit" method="post" class="form-horizontal" enctype="multipart/form-data">
    @await Html.PartialAsync("PartialViews/FormUpdateAndCreateForEmoloyeePartial")

    <div class="form-group row">
        <div class="col-sm-10 offset-sm-2">
            <button type="submit" class="btn btn-primary">Save</button> |
            <a asp-action="Index" class="btn btn active"> Cancel </a>
        </div>
    </div>

</form>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
